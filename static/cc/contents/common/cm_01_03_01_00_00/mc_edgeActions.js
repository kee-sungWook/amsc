/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
   })("stage");
   //Edge symbol end:'stage'

   //=========================================================
   
   //Edge symbol: 'PISTON_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 2000, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("PISTON_MOVE");
   //Edge symbol end:'PISTON_MOVE'

   //=========================================================
   
   //Edge symbol: 'RETURN_SP_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1000, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("RETURN_SP_MOVE");
   //Edge symbol end:'RETURN_SP_MOVE'

   //=========================================================
   
   //Edge symbol: 'CRANK_P1_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("CRANK_P1_MOVE");
   //Edge symbol end:'CRANK_P1_MOVE'

   //=========================================================
   
   //Edge symbol: 'CRANK_P2_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("CRANK_P2_MOVE");
   //Edge symbol end:'CRANK_P2_MOVE'

   //=========================================================
   
   //Edge symbol: 'TIMING_BELT_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 867, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("TIMING_BELT_MOVE");
   //Edge symbol end:'TIMING_BELT_MOVE'

   //=========================================================
   
   //Edge symbol: 'DRIVE_BELT_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 867, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("DRIVE_BELT_MOVE");
   //Edge symbol end:'DRIVE_BELT_MOVE'

   //=========================================================
   
   //Edge symbol: 'GEAR1_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1000, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("GEAR1_MOVE");
   //Edge symbol end:'GEAR1_MOVE'

   //=========================================================
   
   //Edge symbol: 'GEAR2_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1000, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("GEAR2_MOVE");
   //Edge symbol end:'GEAR2_MOVE'

   //=========================================================
   
   //Edge symbol: 'HOT_LIQUID_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1000, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("HOT_LIQUID_MOVE");
   //Edge symbol end:'HOT_LIQUID_MOVE'

   //=========================================================
   
   //Edge symbol: 'L1_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("L1_MOVE");
   //Edge symbol end:'L1_MOVE'

   //=========================================================
   
   //Edge symbol: 'L2_MOVE'
   (function(symbolName) {   
   
   })("L2_MOVE");
   //Edge symbol end:'L2_MOVE'

   //=========================================================
   
   //Edge symbol: 'L3_MOVE'
   (function(symbolName) {   
   
   })("L3_MOVE");
   //Edge symbol end:'L3_MOVE'

   //=========================================================
   
   //Edge symbol: 'L4_MOVE'
   (function(symbolName) {   
   
   })("L4_MOVE");
   //Edge symbol end:'L4_MOVE'

   //=========================================================
   
   //Edge symbol: 'L5_MOVE'
   (function(symbolName) {   
   
   })("L5_MOVE");
   //Edge symbol end:'L5_MOVE'

   //=========================================================
   
   //Edge symbol: 'L6_MOVE'
   (function(symbolName) {   
   
   })("L6_MOVE");
   //Edge symbol end:'L6_MOVE'

   //=========================================================
   
   //Edge symbol: 'L7_MOVE'
   (function(symbolName) {   
   
   })("L7_MOVE");
   //Edge symbol end:'L7_MOVE'

   //=========================================================
   
   //Edge symbol: 'L8'
   (function(symbolName) {   
   
   })("L8");
   //Edge symbol end:'L8'

   //=========================================================
   
   //Edge symbol: 'LIQUIDE_DOT_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1500, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("LIQUIDE_DOT_MOVE");
   //Edge symbol end:'LIQUIDE_DOT_MOVE'

   //=========================================================
   
   //Edge symbol: 'PROPELLER_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("PROPELLER_MOVE");
   //Edge symbol end:'PROPELLER_MOVE'

})(window.jQuery || AdobeEdge.$, AdobeEdge, "EDGE-9031664");