/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
   })("stage");
   //Edge symbol end:'stage'

   //=========================================================
   
   //Edge symbol: 'P_STEERING_P_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 3000, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("P_STEERING_P_MOVE");
   //Edge symbol end:'P_STEERING_P_MOVE'

   //=========================================================
   
   //Edge symbol: 'WATERP_P_TOP_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 750, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("WATERP_P_TOP_MOVE");
   //Edge symbol end:'WATERP_P_TOP_MOVE'

   //=========================================================
   
   //Edge symbol: 'BELT_TOP_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 583, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("BELT_TOP_MOVE");
   //Edge symbol end:'BELT_TOP_MOVE'

   //=========================================================
   
   //Edge symbol: 'SPRAKET_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 750, function(sym, e) {
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(0);

      });
      //Edge binding end

   })("SPRAKET_MOVE");
   //Edge symbol end:'SPRAKET_MOVE'

   //=========================================================
   
   //Edge symbol: 'TENTION_P_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 3000, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("TENTION_P_MOVE");
   //Edge symbol end:'TENTION_P_MOVE'

   //=========================================================
   
   //Edge symbol: 'AIRC_P_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 2500, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("AIRC_P_MOVE");
   //Edge symbol end:'AIRC_P_MOVE'

   //=========================================================
   
   //Edge symbol: 'CRANKSHAFT_P_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 750, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("CRANKSHAFT_P_MOVE");
   //Edge symbol end:'CRANKSHAFT_P_MOVE'

   //=========================================================
   
   //Edge symbol: 'ALTERNATER_P_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 3000, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("ALTERNATER_P_MOVE");
   //Edge symbol end:'ALTERNATER_P_MOVE'

   //=========================================================
   
   //Edge symbol: 'WATERP_P_BOTTOM_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 2000, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("WATERP_P_BOTTOM_MOVE");
   //Edge symbol end:'WATERP_P_BOTTOM_MOVE'

   //=========================================================
   
   //Edge symbol: 'BELT_MIDDLE1_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 250, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("BELT_MIDDLE1_MOVE");
   //Edge symbol end:'BELT_MIDDLE1_MOVE'

   //=========================================================
   
   //Edge symbol: 'BELT_MIDDLE2_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 250, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("BELT_MIDDLE2_MOVE");
   //Edge symbol end:'BELT_MIDDLE2_MOVE'

   //=========================================================
   
   //Edge symbol: 'BELT_BOTTOM_MOVE'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 708, function(sym, e) {
         sym.play(0);

      });
      //Edge binding end

   })("BELT_BOTTOM_MOVE");
   //Edge symbol end:'BELT_BOTTOM_MOVE'

})(jQuery, AdobeEdge, "EDGE-14431557");